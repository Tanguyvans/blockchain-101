# Slides

Beamer slides used to introduce the workshop in person.

## Build

```bash
pdflatex intro.tex
```

Produces `intro.pdf` (3 pages: title + "What is a blockchain?" + "Goal of the workshop").

Compiles with any standard TeX Live / MacTeX / TinyTeX install — no extra packages required.
