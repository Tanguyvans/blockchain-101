# Private key of the wallet that will deploy the contract.
# Export this from MetaMask. NEVER commit the real value.
#
# Use a DISPOSABLE wallet for development — never a wallet that holds real funds.
PRIVATE_KEY=0x...
